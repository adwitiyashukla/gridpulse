# ---------------------------------------------------------------------------
# PUBLIC APP dependencies (Streamlit Community Cloud reads this file).
# Deliberately light so the hosted app stays inside the free tier memory cap.
# For the full pipeline (Dagster, dbt, Airflow, PyTorch) use requirements-dev.txt
# ---------------------------------------------------------------------------
streamlit==1.41.1
duckdb==1.1.3
pandas==2.2.3
numpy==2.1.3
pyarrow==18.1.0
plotly==5.24.1
lightgbm==4.5.0
scikit-learn==1.6.0
httpx==0.28.1
python-dotenv==1.0.1
openai==1.59.6
